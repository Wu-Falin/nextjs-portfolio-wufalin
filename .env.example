# Optional. Without these the pageview counter stays at zero and the rest of the
# site works unchanged.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional. The public origin of the deployed site, used to resolve social cards.
NEXT_PUBLIC_SITE_URL=
